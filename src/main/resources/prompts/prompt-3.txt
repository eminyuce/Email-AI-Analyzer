Enhance the Settings Page to support dynamic system configuration with the following capabilities:

Database Configuration

Allow users to input database connection parameters:

Host

Port

Database Name

Username

Password

Implement a "Test Connection" feature to validate connectivity in real time.

Provide clear success/failure feedback with error details.

LLM Model Configuration

Add support for configuring the active LLM model.

Allow users to input:

Model name (e.g., different Ollama or external models)

Endpoint URL (if applicable)

Optional parameters (temperature, max tokens, etc.)

Ensure the system can dynamically switch between models without redeployment.

Global Configuration Management

Persist all settings (e.g., in DB or config service).

Ensure configurations are loaded at runtime and applied across the application.

Design for extensibility to support additional configuration types in the future.

UX Considerations

Form validation for all inputs

Secure handling of sensitive data (e.g., passwords)

Clean and user-friendly UI